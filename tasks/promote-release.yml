platform: linux
image_resource:
  type: docker-image
  source:
    repository: concourse/buildroot
    tag: git
inputs:
  - name: pipeline
  - name: version
  - name: deployment
outputs:
  - name: deployment-updated
params: 
  MANIFEST_FILE: 

run:
  path: pipeline/tasks/promote-release.sh
